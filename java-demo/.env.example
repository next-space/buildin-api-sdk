# Buildin API 配置示例文件
# 请将此文件复制为 .env 并填入实际的配置值

# API基础路径
# 默认值：https://api.buildin.ai
BUILDIN_API_BASE_PATH=https://api.buildin.ai

# Bearer Token（必须配置）
# 请替换为您的实际API token
BUILDIN_API_BEARER_TOKEN=your-actual-api-token-here

# 使用说明：
# 1. 复制此文件：cp .env.example .env
# 2. 编辑.env文件，填入正确的BUILDIN_API_BEARER_TOKEN
# 3. 运行demo程序即可使用配置的值
# 
# 注意：.env文件已添加到.gitignore，不会被提交到版本控制系统
